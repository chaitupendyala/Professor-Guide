<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chaitanya.project">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/teacher_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/teacher_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login_Page">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".user" />
        <activity android:name=".students" />
        <activity android:name=".attendance" />
        <activity android:name=".marks_entry" />
        <activity android:name=".marks_report" />
        <activity android:name=".attendance_report"></activity>
    </application>

</manifest>